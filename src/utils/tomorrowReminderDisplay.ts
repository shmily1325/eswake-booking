/** UI 用：所有學員皆顯示資料庫中的真實教練名稱。 */
export function displayCoachNameForTomorrowReminder(
  _studentName: string,
  coachName: string,
): string {
  return coachName
}

/** 純文字 LINE 訊息用：所有學員皆顯示資料庫中的真實教練名稱。 */
export function displayCoachNameForTomorrowMessage(
  _studentName: string,
  coachName: string,
): string {
  return coachName
}
